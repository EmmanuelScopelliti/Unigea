const observer1 = new IntersectionObserver(entries => {
    entries.forEach(entry => {
        if (entry.isIntersecting) {
            entry.target.classList.add("animation");
        }
    });
});
observer1.observe(document.querySelector("#img1"));

const observer2 = new IntersectionObserver(entries => {
    entries.forEach(entry => {
        if (entry.isIntersecting) {
            entry.target.classList.add("animation");
        }
    });
});
observer2.observe(document.querySelector("#img2"));

const observer3 = new IntersectionObserver(entries => {
    entries.forEach(entry => {
        if (entry.isIntersecting) {
            entry.target.classList.add("animation");
        }
    });
});
observer3.observe(document.querySelector("#img3"));

const observer4 = new IntersectionObserver(entries => {
    entries.forEach(entry => {
        if (entry.isIntersecting) {
            entry.target.classList.add("animation");
        }
    });
});
observer4.observe(document.querySelector("#img4"));

const observer5 = new IntersectionObserver(entries => {
    entries.forEach(entry => {
        if (entry.isIntersecting) {
            entry.target.classList.add("animation");
        }
    });
});
observer5.observe(document.querySelector("#img5"));

const observer6 = new IntersectionObserver(entries => {
    entries.forEach(entry => {
        if (entry.isIntersecting) {
            entry.target.classList.add("animation");
        }
    });
});
observer6.observe(document.querySelector("#img6"));

const observer7 = new IntersectionObserver(entries => {
    entries.forEach(entry => {
        if (entry.isIntersecting) {
            entry.target.classList.add("animation");
        }
    });
});
observer7.observe(document.querySelector("#img7"));

const observer8 = new IntersectionObserver(entries => {
    entries.forEach(entry => {
        if (entry.isIntersecting) {
            entry.target.classList.add("animation");
        }
    });
});
observer8.observe(document.querySelector("#img8"));

const observer9 = new IntersectionObserver(entries => {
    entries.forEach(entry => {
        if (entry.isIntersecting) {
            entry.target.classList.add("animation");
        }
    });
});
observer9.observe(document.querySelector("#img9"));